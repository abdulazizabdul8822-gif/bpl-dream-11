import React from 'react';

const AvailablePlaters = ({ players }) => {
    console.log(players);
    return (
        <div>
            Available Players
        </div>
    );
};

export default AvailablePlaters;